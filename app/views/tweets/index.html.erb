<%= auto_discovery_link_tag(:atom, url_options = {}, tag_options = {:title => "Twitteiro - Tweets"}) %>
<h3> Mensagems cadastradas </h3>
<div class="mensagens">
  <table>
    <% @tweets.each do |tweet| %>
      <tr>
        <td><%= tweet.tweet %></td>
        <td><%= link_to 'Deletar', tweet, :confirm => 'Tem certeza? Isso é permanente.', :method => :delete %></td>
      </tr>
    <% end %>
  </table>
</div>
<br/>

<%= link_to 'Nova mensagem', new_tweet_path %>
