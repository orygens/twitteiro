require 'spec_helper'

describe RetuitesController do

  def mock_supportship(stubs={})
    @mock_supportship ||= mock_model(Retuite, stubs).as_null_object
  end

  describe 'specs do RetuitesController' do
    it 'specs' do
    end
  end
end
